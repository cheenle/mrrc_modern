# IC-7300MK2 CI-V 协议知识库

> 从 Icom《IC-7300MK2 CI-V REFERENCE GUIDE》(A7841-8EX, ©2025 Icom Inc.)
> 提取并整理。 面向 mrrc_modern 的 `backends/ic7300/` CI-V 后端。
> 源文件: `IC-7300MK2_ENG_CI-V_0.pdf`(仓库根目录)。
> 整理日期: 2026-08-18。 每个字节格式均对照 PDF 原文核实; 与当前代码实现的
> 逐条比对见文末「§7 实现符合性验证」。

---

## 1. 通信参数 (Communication Parameters)

| 项 | 值 | 说明 |
|----|----|------|
| 传输方式 | CI-V (USB 虚拟串口, 或 LAN) | IC-7300MK2 后面板 [USB] 口提供 **两个** 虚拟 COM 口 (USB A / USB B) |
| 默认 CI-V 地址 | **`B6` (0xB6, 182)** | ⚠️ 与 IC-7300 的默认 `0x94` **不同** |
| 控制器地址 | `E0` (0xE0, 224) | Icom 标准 PC 地址 |
| 波特率 | USB: 115200 (典型); [REMOTE] 口需配置 | 见基本手册 |
| 数据格式 | 8N1, 无流控 | CI-V 串口 |
| 结束码 | `FD` | 消息结束 |
| 应答 | `FB` = OK, `FA` = NG | 收到命令后电台回 `FE FE <from> <to> FB/FA FD` |
| 回声 | 同总线每个帧都被电台回显 | 读侧需丢弃自己的帧 (from == 0xE0) |

> ⚠️ **[REMOTE] 口开机命令的特殊要求**: 通过 [REMOTE] 口发送开机命令
> (18 01) 时, 必须连续输入多个 `FE` 前缀, 数量取决于波特率
> (19200→25 个, 9600→13 个, 4800→7 个)。 通过 **USB** 口则不需要。
> 本项目走 USB 串口, 不受此限制。

---

## 2. 帧格式 (Frame Format)

```
控制器 → 电台:   FE FE  B6  E0  Cn  [Sc]  [Data]  FD
电台 → 控制器:   FE FE  E0  B6  Cn  [Sc]  [Data]  FD
                  │    └─┬─┘ └─┬─┘ └┬┘
                  └ 前导码   │    │    └ 命令号 Cn
                    电台地址 │    └ 控制器地址 E0
                  (to)      (from)
```

- `FE FE` = 固定前导码。
- 电台地址 `B6` (MK2 默认), 控制器地址 `E0`。
- 带 `*` 的命令可用**无 sub/data 的查询**读取当前设置 (部分需带 sub 指定通道/项目)。
- 数据区按命令不同采用 BCD 或枚举。

---

## 3. 频率 / 模式格式 (Frequency & Mode Formats)

### 3.1 工作频率 — 5 字节 BCD, **低位在前**

命令: `00` (发送/查询), `03` (读), `05` (写), `1C 03` (发射频率)

线上字节顺序 (byte1 在前):

```
byte1    byte2    byte3    byte4    byte5
[10Hz|1Hz]  [1kHz|100Hz]  [100kHz|10kHz]  [10MHz|1MHz]  [1GHz|100MHz]
 0~9 0~9      0~9   0~9      0~9    0~9       0~7    0~9      0(固定) 0(固定)
```

- 100 MHz 位、1 GHz 位固定为 0 (电台工作范围 0.03–74.8 MHz)。
- 10 MHz 位取值 0–7。
- 分辨率 1 Hz。
- 例: `14_074_000` Hz → `00 40 07 14 00`
  (byte1=00 [10Hz|1Hz], byte2=40 [1kHz=4|100Hz=0], byte3=07 [100kHz=0|10kHz=7],
   byte4=14 [10MHz=1|1MHz=4], byte5=00 [1GHz|100MHz])。

> ⚠️ **与老 Icom 设备的区别**: 这是 **LSB-pair-first**(最低两位在前)。
> 项目 `civ_codec.encode_freq_bcd` / `decode_freq_bcd` 即按此实现。

### 3.2 工作模式 — 模式字节 + 滤波器字节

命令: `01` (发送/查询), `04` (读), `06` (写); 响应/写入 2 字节。

| 字节 1 (模式) | 含义 | 字节 2 (滤波器) |
|---|---|---|
| 00 | LSB | 01 = FIL1 |
| 01 | USB | 02 = FIL2 |
| 02 | AM | 03 = FIL3 |
| 03 | CW | — |
| 04 | RTTY | — |
| 05 | FM | — |
| 07 | CW-R | — |
| 08 | RTTY-R | — |

- 命令 01/06 的滤波器字节可省略, 省略时 01 选 FIL1、06 用该模式的默认滤波器。
- **MK2 表中没有模式码 06** (IC-7300 的 WFM 为仅接收, MK2 无此表项)。
- 项目命名: 03→"CW-U"、07→"CW-L"、04→"RTTY-L"、08→"RTTY-U"
  (复用共享 UI 命名, 与 FT-710 侧 CW-U/CW-L/RTTY-L/RTTY-U 对齐)。

---

## 4. 电平 / 仪表格式 (Level & Meter Formats)

### 4.1 电平命令 (cmd `14`) — 2 字节 BCD, **高位在前**

`FE FE B6 E0 14 <选择器> <2字节BCD> FD`, 值域 `00 00`–`02 55` (0–255)。

| 选择器 | 项 | 选择器 | 项 |
|---|---|---|---|
| 01 | AF 增益 | 0A | RF 功率 |
| 02 | RF 增益 | 0B | MIC 增益 |
| 03 | 静噪 | 0C | 键速 (6–48 WPM) |
| 05 | APF 峰值频率 | 0D | Notch 设置 |
| 06 | NR 电平 | 0E | 语音压缩器电平 |
| 07 | PBT1 (内) | 0F | 破音延迟 (02 55=13.0d) |
| 08 | PBT2 (外) | 12 | NB 电平 |
| 09 | CW 音调 (5 Hz 步进) | | |

### 4.2 仪表命令 (cmd `15`) — 2 字节 BCD 读数

`FE FE B6 E0 15 <选择器> FD` → 应答 2 字节 BCD (0–255)。

| 选择器 | 仪表 | 校准参考点 (BCD) |
|---|---|---|
| 01 | SQL 开/闭 (00/01) | — |
| 02 | S 表 | S0=00, S9=01 20 (120), S9+60=02 41 (241) |
| 05 | 综合静噪开/闭 (00/01) | — |
| 07 | OVF 指示 (00/01) | — |
| 11 | Po 功率 | 0%=00, 50%=01 43 (143), 100%=02 13 (213) |
| 12 | SWR | 1.0=00, 1.5=00 48 (48), 2.0=00 80 (80), 3.0=01 20 (120) |
| 13 | ALC | 最小=00, 最大=01 20 (120) |
| 14 | COMP | 0 dB=00, 15 dB=01 30 (130), **30 dB=02 10 (210)** |
| 15 | Vd 电压 | 0 V=00, **10 V=00 13 (19)**, 16 V=02 41 (241) |
| 16 | Id 电流 | 10 A=00 97 (97), 15 A=01 46 (146), 25 A=02 41 (241) |

> 除 SSB/CW/RTTY 模式外的仪表, 接收时读数为 00 00。

### 4.3 开关命令 (cmd `16`) — 选择器 + 枚举

| 选择器 | 项 | 数据 |
|---|---|---|
| 02 | 前置放大 | 00=OFF, 01=P.AMP1, 02=P.AMP2 |
| 12 | AGC 时间常数 | **01=FAST, 02=MID, 03=SLOW** (表内无 00) |
| 22 | NB 噪声抑制 | 00/01 |
| 32 | APF | 00=OFF, 01=WIDE, 02=MID, 03=NAR |
| 40 | NR 降噪 | 00/01 |
| 41 | ANF 自动陷波 | 00/01 |
| 42 | 中继音 | 00/01 |
| 43 | 音调静噪 | 00/01 |
| 44 | 语音压缩器 | 00/01 |
| 45 | Monitor | 00/01 |
| 46 | VOX | 00/01 |
| 47 | CW 破音 | 00=OFF, 01=Semi, 02=Full |
| 48 | 手动 Notch | 00/01 |
| 4F | Twin Peak 滤波 | 00/01 |
| 50 | Lock | 00/01 |
| 56 | IF 滤波器形状 | 00=SHARP, 01=SOFT |
| 57 | 手动 Notch 带宽 | 00=WIDE, 01=MID, 02=NAR |
| 58 | SSB 发射滤波带宽 | 00=WIDE, 01=MID, 02=NAR |
| 65 | IP Plus | 00/01 |
| 66 | TX Inhibit | 00/01 |

---

## 5. 本后端用到的其他命令

| 命令 | 说明 | 数据格式 |
|---|---|---|
| 07 | VFO 模式 | 00=选 VFO-A, 01=选 VFO-B, A0=AB 平衡, B0=AB 交换 |
| 0F | Split | 00=OFF, 01=ON (收发异频) |
| 11 | 20 dB 衰减器 | **00=OFF, 20=ON** (⚠️ ON 是 0x20, 不是 01) |
| 18 | 电源开关 | **00=关, 01=开** (Set 命令; 表内无 `*`, 未定义读回) |
| 1A 05 | Set 菜单区 | 见 §6 差异 (CI-V Transceive 项) |
| 1C 00 | PTT | 00=RX, 01=TX |
| 1C 01 | 内部天调 | 00=OFF, 01=ON, **02=开始调谐** (电台自动发射载波自调) |
| 1C 03 | 发射频率 | 读, 5 字节 BCD (同 §3.1); 不可写 |
| 25 | 选中/未选中 VFO 频率 | 数据尾部 00=Selected, 01=Unselected |
| 27 | 频谱示波器 | 见 §7 |

> 说明: 命令 02 (频带边缘读)、1E (TX 频带信息)、19 (读 ID)、17 (CW 消息)、
> 1B (CTCSS 频率)、21 (RIT)、26 (VFO 模式+滤波器)、28 (语音 TX 记忆)
> 在本后端未实现 — 不是协议错误, 只是未用到。

---

## 6. 频谱示波器 (cmd `27`)

### 6.1 示波器子命令

| 子命令 | 说明 | 数据 |
|---|---|---|
| 10 | 频谱显示开关 | 00/01 |
| 11 | **波形数据输出开关** | 00/01 (真正启动 27 00 数据流的开关) |
| 12 | Main/Sub 作用域 | 00 (Main only) |
| 13 | Single/Dual 作用域 | 00 (Single only) |
| 14 | 作用域模式 | 00=Center, 01=Fixed, 02=SCROLL-C, 03=SCROLL-F |
| 15 | Span (Center/SCROLL-C) | **5 字节 BCD 半跨度频率** |
| 16 | Edge 编号 (Fixed/SCROLL-F) | 01–04 |
| 17 | Hold | 00/01 |
| 19 | 参考电平 | –20.0 ~ +20.0 dB (0.5 dB 步进) |
| 1A | 扫描速度 | 00=FAST, 01=MID, 02=SLOW |
| 1B | TX 中显示 (CENTER) | 00/01 |
| 1C | CENTER 类型 | 00=Filter Center, 01=Carrier Point, 02=Abs. Freq. |
| 1D | VBW | 00=Narrow, 01=Wide |
| 1E | Fixed 边频率 | 频率范围 01–13 + Edge 01–04 + 2×5 字节 BCD |
| 20 | Marker 位置 (FIX/SCROLL) | 00=Filter Center, 01=Carrier Point |

**Span 值 (27 15 数据, 半跨度)**: `2500`(±2.5k) `5000`(±5k) `10000`(±10k)
`25000`(±25k) `50000`(±50k) `100000`(±100k) `250000`(±250k) `500000`(±500k)。
线上为 5 字节 BCD 频率 (如 ±100 kHz → `00 00 10 00 00`)。

### 6.2 波形数据 (27 00) — 分片流

```
data[0]  子命令 00
data[1]  接收器: 00=Main (IC-7300MK2 单接收)
data[2]  分片序号 (BCD): 01–11
data[3]  分片总数 (BCD): 11=USB, 01=LAN
序1:     data[4] 作用域模式, data[5:10] 中心/低边频率 BCD,
         data[10:15] 半跨度/高边频率 BCD, data[15] 越界 (00/01)
序>=2:   data[4:] 波形数据 (0–160)
```

- 完整波形 = **475 个 bin**, 取值 `00`–`A0` (0–160)。
- USB: 分 11 段 — 第 1 段只含信息 (15 B), 第 2–10 段各 50 bin (53 B),
  第 11 段 25 bin (28 B)。 (9×50 + 25 = 475)
- LAN: 1 段, 490 B (15 信息 + 475 波形)。
- 越界 (01) 时波形数据省略。
- 项目 `civ_codec.parse_scope_segment` / `ScopeAssembler` / `scale_scope_bins`
  (0–160 → 0–255) / `upsample_bins` (475 → 850) 与上述完全一致。

---

## 7. IC-7300MK2 与 IC-7300 的差异 (对本后端重要)

| 项 | IC-7300 | **IC-7300MK2** |
|---|---|---|
| 默认 CI-V 地址 | `0x94` | **`0xB6`** |
| CI-V Transceive Set 菜单项 | `1A 05 00 71` | **`1A 05 00 89`** |
| USB 虚拟 COM | 1 个 | **2 个 (USB A / USB B)**, 均可选作 CI-V / SEND / Keying |
| 网络功能 | — | LAN 口 + 网络遥控 (DHCP/IP/端口/远程设置) |
| 模式码 06 (WFM) | 有 (仅接收) | 表中无 |
| [REMOTE] 口 | — | 存在 (开机命令需多 FE 前缀) |

> ⚠️ 关键差异: 对 MK2 发送 `1A 05 00 71` 会被解释为 **AF Output Level 设置**,
> 而不是 CI-V Transceive。 启用 MK2 的 Transceive 必须用 `1A 05 00 89 01`。

---

## 8. 实现符合性验证 (Implementation Conformance)

> 逐条对照 `backends/ic7300/` 当前代码与上面 MK2 协议事实。
> 验证基于代码阅读 + 现有单测 (`tests/test_civ_codec.py` 频率 BCD 向量
> `14_074_000 → 00 40 07 14 00` 与 PDF 一致)。 **尚未实机验证。**

### 8.1 ✅ 符合 (Conforms)

| # | 项 | 代码位置 | 结论 |
|---|---|---|---|
| 1 | 频率 5 字节 BCD 低位在前 | `civ_codec.encode/decode_freq_bcd`; `civ_controller.set/get_frequency` | ✅ |
| 2 | 模式码 + FIL 字节 | `set_mode`/`get_mode`/`set_filter_width` (06/04) | ✅ |
| 3 | 电平选择器 14 (01/02/03/06/0A/0B/0E/12) | `LVL_*` 常量 + `_level_set` | ✅ |
| 4 | 仪表选择器 15 (02/11/12/13/14/15/16) | `METER_SUB_*` + `get_meter` | ✅ |
| 5 | 开关选择器 16 (02/12/22/40/44) | `SW_*` + `_switch_set` | ✅ |
| 6 | 衰减器 11 (00/20) | `set_attenuator` | ✅ |
| 7 | 电源 18 (00/01) | `set_power` | ✅ |
| 8 | PTT/天调 1C (00/01, 00/01/02) | `set_ptt`/`set_tune`/`set_tuner` | ✅ |
| 9 | VFO 07、Split 0F | `set_vfo`/`set_split` | ✅ |
| 10 | 示波器子命令 10/11/14/15/1A | `set_scope_*` | ✅ |
| 11 | 示波器波形解析 (序/总数/模式/频率/越界/bin) | `parse_scope_segment`/`ScopeAssembler` | ✅ |
| 12 | S 表 (S9=120, +60=241) | `S_METER_CAL` | ✅ |
| 13 | Po (50%=143, 100%=213)、SWR (48/80/120)、ALC (120)、Id (97/146/241) | 各 `*_CAL` | ✅ |
| 14 | 频谱跨度 = 5 字节 BCD 半跨度, 数值匹配 | `SCOPE_SPAN_HZ` + `set_scope_span` | ✅ |

### 8.2 ❌ 差异 / 缺口 (Discrepancies / Gaps)

> 状态: A–E 已于 2026-08-18 修复 (见各建议列); F–I 仍为待办/提示。

| # | 级别 | 问题 | 影响 | 状态 |
|---|---|---|---|---|
| A | **高 (仅 MK2)** | `IC7300MK2Backend` 未覆盖 CI-V 地址, 仍用 `0x94`; MK2 默认为 `0xB6` | 对 MK2 根本连不上 (除非 `IC7300_CIV_ADDR=0xB6` 环境变量) | ✅ 已修: `IC7300MK2Backend.__init__` 传 `civ_addr=MK2_CIV_ADDR (0xB6)`; 可用 `IC7300MK2_CIV_ADDR` 覆盖 |
| B | **高 (仅 MK2)** | 连接时发送 `1A 05 00 71 01` 启 Transceive; MK2 该项是 AF Output Level, 真项为 `1A 05 00 89 01` | 对 MK2 前板频率/模式变更不再广播, 且误设 AF 输出电平 | ✅ 已修: `CivController` 按地址选 `SETMODE_CIV_TRANSCEIVE_MK2 (00 89)` |
| C | **中** | `server.py _power_on_radio()` 用 Yaesu `FA` 查询验证开机, 对所有后端统一调用; 对 CI-V, `0xFA` 是 NG 应答码, 非查询命令 | IC-7300 走 WS 电源路径时开机验证失效 (仅维护脚本用) | ✅ 已修: `_power_on_radio(cat, backend)` 走 `backend.boot_verify()`; `IC7300Backend.boot_verify` 查 `03` |
| D | **低** | `COMP_CAL` 最高点 `(241, 30.0)`; MK2 参考为 30 dB = BCD `02 10` (210) | TX 中 COMP 表显示略偏高 | ✅ 已修: 改 `(210, 30.0)` (建议实机确认) |
| E | **低** | `VOLTAGE_CAL` 中点 `(13, 10.0)`; MK2 参考为 10 V = BCD `00 13` (19) | 仅表格, Vd/Id 未轮询 (`has_vd_id_meters=False`) | ✅ 已修: 改 `(19, 10.0)` |
| F | **低** | `set_agc` 允许 `0` (OFF); MK2 `16 12` 只定义 01/02/03, OFF 走 `1A 04` | 传 00 无参考定义 | 待办: 如 UI 需要 OFF, 改用 `1A 04`; 否则限制 1–3 |
| G | **低/待实机** | `power_on` 轮询用裸 `18` 查询; 参考表中 `18` 无 `*`(未定义读回) | 可能无应答 | 待实机: 不可用则改从 `03` 有应答推断开机 |
| H | 提示 | `set_auto_notch` 故意置空 (`has_auto_notch=False`), 但 MK2 存在 ANF (`16 41`) | UI 隐藏开关, 非协议错误 | 需要时启用 |
| I | 提示 | 模式码 `06` (WFM) 仅 IC-7300 有; MK2 表无 | 后端含 06 但不进 `UI_MODES_IC7300`, 不会误发 | — |

### 8.3 建议的实机验证清单

1. 对 MK2 用地址 `B6` 连接, 确认 ID / 频率读写。
2. 发 `1A 05 00 89 01` 后前板旋钮是否触发 00/01 广播。
3. 裸 `18` 查询是否返回当前电源状态 (G)。
4. COMP/Vd 校准点 (D/E) 实读对比。
5. `16 12 00` (AGC OFF) 的行为 (F)。

---

## 9. 已知陷阱 (Known Pitfalls)

1. **频率 BCD 低位在前** — 不要照搬老 Icom 手册写反字节序; 例 14.074 MHz =
   `00 40 07 14 00`。
2. **电平/仪表是 2 字节 BCD 高位在前** — 255 = `02 55`, 与频率的方向相反。
3. **衰减器 ON = `0x20`**, 不是 01。
4. **天调开始调谐 = `1C 01 02`**, 电台会自己发射载波。
5. **Scope span 是半跨度** — 前端频率轴按 `center - half_span` 显示左边缘。
6. **27 00 分片流**: 序/总数是 BCD 编码 (11 段 → 字节 `0x11`); 波形越界时省略
   波形数据。
7. **CI-V 无流控**; 同总线回声需按 `from == 0xE0` 丢弃。
8. **双虚拟 COM (MK2)**: 同一终端不能同时用于 SEND 和 Keying 设置; 读 CI-V
   走 USB (A)。
9. **[REMOTE] 口开机需要多 FE 前缀** (仅该口); USB 口不需要。

---

## 10. 参考资料 (References)

- Icom《IC-7300MK2 CI-V REFERENCE GUIDE》A7841-8EX (Oct 2025) — 本文全部
  字节格式的来源 (仓库根 `IC-7300MK2_ENG_CI-V_0.pdf`)。
- `backends/ic7300/civ_codec.py` — 帧/BCD/示波器解析 (纯函数)。
- `backends/ic7300/civ_controller.py` — 命令实现。
- `backends/ic7300/config_ic7300.py` — 地址/模式/滤波/校准/跨度表。
- wfview `rigs/IC-7300.rig` 与 hamlib `rigs/icom/ic7300.c` — 交叉参照
  (MK2 地址 `0xB6`、Transceive 项 `0089` 亦见 hamlib 注释)。
