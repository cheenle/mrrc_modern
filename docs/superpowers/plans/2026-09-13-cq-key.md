# CQ 键（服务端 CQ 呼叫）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。规格：`docs/superpowers/specs/2026-09-13-cq-key-design.md`。

**目标：** Web 界面加一个 CQ 键：按一下由**服务端**键控电台、播放打包的 CQ 录音一遍、自动解除，并向所有客户端广播 `complete`；再按可重播，播放中再按可中止。

**架构：** 新模块 `cq_player.py` 负责资产加载（48 kHz 单声道 16-bit、20 ms 分帧）与播放循环（20 ms tick、仅在设备队列浅于 4 帧时喂 `AudioHandler.feed_tx_audio`）；`server.py` 提供键控/解除回调（复用 PTT 的门禁 + TX 归属 + `start_tx`/`stop_tx(graceful)` 顺序）并处理 `set{field:"cq"}`、`cqState` 广播与断线中止。不新增发射通路。

**技术栈：** Python asyncio + numpy、标准库 `wave`、既有 `audio_resample.resample_pcm`；前端 vanilla JS（`static/index.html` + `ft710_ui.js` + `ft710.css`）；测试 stdlib `unittest`（无硬件）。

---

## 文件结构

| 文件 | 职责 |
| --- | --- |
| `cq_player.py`（新建） | 资产解析/归一化/分帧 + `CQPlayer`（状态机、播放任务、`status()`） |
| `audio_handler.py`（修改） | 新增 `tx_queue_frames()`（队列深度查询，供背压使用） |
| `server.py`（修改） | `set{field:"cq"}` 处理、`_cq_key`/`_cq_unkey`、`_broadcast_cq_state`、`fullState.cq`、mic 帧互斥、断线中止、启动就绪日志 |
| `config.py`（修改） | `MRRC_CQ_FILE`（默认空 → `static/audio/cq.wav`） |
| `static/audio/cq.wav`（新建资产） | 由根目录 `CQCQ.m4a` 转换（48 kHz 单声道 16-bit） |
| `static/index.html` / `static/ft710_ui.js` / `static/ft710.css`（修改） | CQ 按钮、点击发送、`cqState` 渲染、样式 |
| `static/ft710_main.js`（修改） | 摄取 `cqState`（照 `recordingState` 模式） |
| `tests/test_cq_player.py`（新建） | 资产解析 + 播放状态机（假 audio/键控回调） |
| `tests/test_cq_server.py`（新建） | 命令面、安全护栏、广播、断线中止 |
| `tests/test_audio.py`（修改） | `tx_queue_frames()` 契约 |
| `tests/test_server_ws_protocol.py`（修改） | 前端 source-contract 断言（按钮/命令/渲染） |
| 文档（修改） | CHANGELOG / README / AGENTS / SDD / OPERATION_GUIDE / tests-README / 网站指南 |

---

### 任务 1：CQ 资产

**文件：** 创建 `static/audio/cq.wav`

- [ ] **步骤 1：转换（单声道 48 kHz 16-bit，长度 6.22 s）**

```bash
cd /Users/cheenle/HAM/mrrc_modern
mkdir -p static/audio
ffmpeg -y -i CQCQ.m4a -ac 1 -ar 48000 -c:a pcm_s16le static/audio/cq.wav
ffprobe -v error -show_entries stream=codec_name,sample_rate,channels,bits_per_sample \
        -show_entries format=duration -of default=noprint_wrappers=1 static/audio/cq.wav
```

预期：`codec_name=pcm_s16le`、`sample_rate=48000`、`channels=1`、`bits_per_sample=16`、`duration≈6.22`

- [ ] **步骤 2：Commit**

```bash
git add static/audio/cq.wav
git commit -m "feat(cq): add the packaged CQ recording (mono 48 kHz PCM from CQCQ.m4a)"
```

---

### 任务 2：`AudioHandler.tx_queue_frames()`（背压查询）

**文件：** 修改 `audio_handler.py`（在 `has_pending_tx_audio()` 附近）、测试 `tests/test_audio.py`

- [ ] **步骤 1：编写失败的测试**

```python
class TxQueueDepthTests(unittest.TestCase):
    def test_tx_queue_frames_reports_queued_frames(self):
        h = AudioHandler(rx_rate=RATE, tx_rate=RATE)
        h._tx_stream = object()                      # pretend the output is open
        self.assertEqual(h.tx_queue_frames(), 0)
        h.feed_tx_audio(b"\x00\x00" * 960)           # one 20 ms frame
        self.assertEqual(h.tx_queue_frames(), 1)

    def test_tx_queue_frames_is_zero_without_a_stream(self):
        h = AudioHandler(rx_rate=RATE, tx_rate=RATE)
        h.feed_tx_audio(b"\x00\x00" * 960)           # dropped: no stream
        self.assertEqual(h.tx_queue_frames(), 0)
```

- [ ] **步骤 2：运行确认失败**

运行：`.venv/bin/python -m unittest tests.test_audio.TxQueueDepthTests -v`
预期：FAIL — `AttributeError: 'AudioHandler' object has no attribute 'tx_queue_frames'`

- [ ] **步骤 3：实现**

```python
    def tx_queue_frames(self) -> int:
        """Frames waiting for the DAC (backpressure signal for server-side audio).

        The CQ player feeds one 20 ms frame per tick while this stays shallow,
        so a slow device makes the player wait instead of hitting the drop cap.
        """
        with self._tx_lock:
            return len(self._tx_queue)
```

- [ ] **步骤 4：运行确认通过**

运行：`.venv/bin/python -m unittest tests.test_audio.TxQueueDepthTests -v` → PASS

- [ ] **步骤 5：Commit**

```bash
git add audio_handler.py tests/test_audio.py
git commit -m "feat(audio): expose TX queue depth for server-side audio producers"
```

---

### 任务 3：`cq_player.py` 资产加载

**文件：** 创建 `cq_player.py`、测试 `tests/test_cq_player.py`

- [ ] **步骤 1：编写失败的测试**

```python
"""Tests for cq_player.py — asset loading and the CQ call state machine."""
import asyncio
import tempfile
import unittest
import wave
from pathlib import Path

from cq_player import CQPlayer, CQUnavailable


def make_wav(path: Path, seconds: float, rate: int = 48_000, channels: int = 1,
             value: int = 1000, width: int = 2) -> Path:
    frames = int(seconds * rate)
    with wave.open(str(path), "wb") as w:
        w.setnchannels(channels)
        w.setsampwidth(width)
        w.setframerate(rate)
        w.writeframes(b"".join(
            value.to_bytes(width, "little", signed=True) * channels
            for _ in range(frames)))
    return path


class CQAssetTests(unittest.TestCase):
    def test_mono_48k_is_loaded_frame_aligned(self):
        with tempfile.TemporaryDirectory() as tmp:
            p = make_wav(Path(tmp) / "cq.wav", 0.1)              # 100 ms
            player = CQPlayer(asset_path=p)
            self.assertTrue(player.load())
            self.assertEqual(player.status()["duration_s"], 0.1)
            self.assertEqual(player.status()["frames_total"], 5)  # 5 × 20 ms

    def test_stereo_is_mixed_down(self):
        with tempfile.TemporaryDirectory() as tmp:
            p = make_wav(Path(tmp) / "cq.wav", 0.02, channels=2)
            player = CQPlayer(asset_path=p)
            self.assertTrue(player.load())
            self.assertEqual(player.status()["frames_total"], 1)

    def test_44100_is_resampled_to_48k(self):
        with tempfile.TemporaryDirectory() as tmp:
            p = make_wav(Path(tmp) / "cq.wav", 0.1, rate=44_100)
            player = CQPlayer(asset_path=p)
            self.assertTrue(player.load())
            self.assertAlmostEqual(player.status()["duration_s"], 0.1, places=2)

    def test_partial_tail_is_padded_to_a_full_frame(self):
        with tempfile.TemporaryDirectory() as tmp:
            p = make_wav(Path(tmp) / "cq.wav", 0.035)             # 35 ms → 2 frames
            player = CQPlayer(asset_path=p)
            self.assertTrue(player.load())
            self.assertEqual(player.status()["frames_total"], 2)

    def test_missing_asset_reports_an_actionable_reason(self):
        player = CQPlayer(asset_path=Path("/nonexistent/cq.wav"))
        self.assertFalse(player.load())
        self.assertIn("not found", player.unavailable_reason.lower())

    def test_unsupported_sample_width_is_rejected(self):
        with tempfile.TemporaryDirectory() as tmp:
            p = make_wav(Path(tmp) / "cq.wav", 0.02, width=1)     # 8-bit
            player = CQPlayer(asset_path=p)
            self.assertFalse(player.load())
            self.assertIn("16-bit", player.unavailable_reason)

    def test_too_long_asset_is_rejected(self):
        with tempfile.TemporaryDirectory() as tmp:
            p = make_wav(Path(tmp) / "cq.wav", 1.0)
            player = CQPlayer(asset_path=p, max_seconds=0.5)
            self.assertFalse(player.load())
            self.assertIn("too long", player.unavailable_reason)

    def test_truncated_file_is_rejected_without_raising(self):
        with tempfile.TemporaryDirectory() as tmp:
            p = Path(tmp) / "cq.wav"
            p.write_bytes(b"RIFF\x00\x00\x00\x00WAVEjunk")
            player = CQPlayer(asset_path=p)
            self.assertFalse(player.load())
            self.assertTrue(player.unavailable_reason)
```

- [ ] **步骤 2：运行确认失败**

运行：`.venv/bin/python -m unittest tests.test_cq_player -v`
预期：FAIL — `ModuleNotFoundError: No module named 'cq_player'`

- [ ] **步骤 3：实现（模块骨架 + 资产加载）**

```python
"""Server-side CQ call playback (spec 2026-09-13 §4).

One call = one transmission: the caller supplies key/unkey callbacks (server.py
owns the TX gate, ownership and drain sequence), this module feeds the packaged
recording into the existing device-domain TX queue and reports its state.
"""
from __future__ import annotations

import asyncio
import logging
import wave
from pathlib import Path
from typing import Awaitable, Callable, Optional

import numpy as np

from audio_resample import resample_pcm

logger = logging.getLogger("mrrc.cq")

CODEC_RATE = 48_000
FRAME_SAMPLES = 960                       # 20 ms at 48 kHz — same frame as Opus TX
CQ_MAX_SECONDS = 30.0
CQ_LOW_WATER_FRAMES = 4                   # feed while the device queue is this shallow
_TICK_S = FRAME_SAMPLES / CODEC_RATE


class CQUnavailable(Exception):
    """A call cannot start; ``str(exc)`` is operator-facing text."""


class CQPlayer:
    """Loads one CQ asset and plays it once per call."""

    def __init__(self, *, asset_path: Optional[Path] = None,
                 max_seconds: float = CQ_MAX_SECONDS,
                 audio=None,
                 key: Optional[Callable[[Optional[str]], Awaitable[bool]]] = None,
                 unkey: Optional[Callable[[bool], Awaitable[None]]] = None,
                 is_transmitting: Optional[Callable[[], bool]] = None,
                 on_change: Optional[Callable[[dict], Awaitable[None]]] = None):
        self._asset_path = asset_path
        self._max_seconds = float(max_seconds)
        self._audio = audio
        self._key = key
        self._unkey = unkey
        self._is_transmitting = is_transmitting
        self._on_change = on_change
        self._frames: list[bytes] = []
        self._ready = False
        self._reason = "asset not loaded"
        self._task: Optional[asyncio.Task] = None
        self._state = "idle"
        self._sent = 0
        self._started_by: Optional[str] = None
        self._owner_token: Optional[str] = None
        self._reason_code: Optional[str] = None

    # ── asset ─────────────────────────────────────────────────────
    @property
    def ready(self) -> bool:
        return self._ready

    @property
    def unavailable_reason(self) -> str:
        return self._reason

    def load(self) -> bool:
        """Read + normalise the asset. Returns False (with a reason) on failure."""
        path = self._asset_path
        try:
            if path is None or not Path(path).is_file():
                raise ValueError(f"CQ asset not found: {path}")
            with wave.open(str(path), "rb") as w:
                width, channels, rate = w.getsampwidth(), w.getnchannels(), w.getframerate()
                frames = w.readframes(w.getnframes())
            if width != 2:
                raise ValueError(f"CQ asset must be 16-bit PCM (got {width * 8}-bit)")
            if rate <= 0 or channels < 1:
                raise ValueError("CQ asset has an invalid rate/channel count")
            samples = np.frombuffer(frames, dtype="<i2")
            if channels > 1:
                samples = samples.reshape(-1, channels).mean(axis=1)
            samples = samples.astype("<i2")
            pcm = samples.tobytes()
            if rate != CODEC_RATE:
                pcm = resample_pcm(pcm, rate, CODEC_RATE)
                samples = np.frombuffer(pcm, dtype="<i2")
            duration = samples.size / CODEC_RATE
            if duration <= 0:
                raise ValueError("CQ asset is empty")
            if duration > self._max_seconds:
                raise ValueError(
                    f"CQ asset too long: {duration:.1f}s > {self._max_seconds:.0f}s limit")
            frames_list: list[bytes] = []
            for start in range(0, samples.size, FRAME_SAMPLES):
                chunk = samples[start:start + FRAME_SAMPLES]
                if chunk.size < FRAME_SAMPLES:          # zero-pad the tail
                    chunk = np.concatenate(
                        [chunk, np.zeros(FRAME_SAMPLES - chunk.size, dtype="<i2")])
                frames_list.append(np.ascontiguousarray(chunk, dtype="<i2").tobytes())
            self._frames = frames_list
            self._duration_s = len(frames_list) * _TICK_S
        except Exception as exc:                        # any parse failure is a refusal
            self._frames = []
            self._ready = False
            self._reason = f"CQ asset unusable: {exc}"
            logger.warning("%s", self._reason)
            return False
        self._ready = True
        self._reason = ""
        logger.info("CQ ready: %s (%.1f s, 48 kHz mono, %d frames)",
                    path, self._duration_s, len(self._frames))
        return True
```

- [ ] **步骤 4：运行确认通过**

运行：`.venv/bin/python -m unittest tests.test_cq_player -v` → PASS（8 项）

- [ ] **步骤 5：Commit**

```bash
git add cq_player.py tests/test_cq_player.py
git commit -m "feat(cq): CQ asset loading (mono 48 kHz, frame-aligned, validated)"
```

---

### 任务 4：`CQPlayer` 播放状态机

**文件：** 修改 `cq_player.py`（追加方法）、测试 `tests/test_cq_player.py`

- [ ] **步骤 1：编写失败的测试（假 audio/键控 + 生命周期）**

```python
class _FakeAudio:
    def __init__(self):
        self.frames = []

    def feed_tx_audio(self, pcm):
        self.frames.append(pcm)

    def tx_queue_frames(self):
        return 0                     # device is never behind in tests


class CQPlaybackTests(unittest.IsolatedAsyncioTestCase):
    def _player(self, tmp, seconds=0.1, transmitting=True):
        calls = {"key": 0, "unkey": [], "broadcasts": []}
        state = {"tx": transmitting}

        async def key(token):
            calls["key"] += 1
            state["tx"] = True
            return True

        async def unkey(graceful):
            calls["unkey"].append(graceful)
            state["tx"] = False

        async def on_change(snap):
            calls["broadcasts"].append(snap["state"])

        audio = _FakeAudio()
        p = make_wav(Path(tmp) / "cq.wav", seconds)
        player = CQPlayer(asset_path=p, audio=audio, key=key, unkey=unkey,
                          is_transmitting=lambda: state["tx"], on_change=on_change)
        player.load()
        return player, audio, calls, state

    async def test_one_shot_call_keys_plays_and_unkeys(self):
        with tempfile.TemporaryDirectory() as tmp:
            player, audio, calls, state = self._player(tmp, seconds=0.1)   # 5 frames
            await player.start(started_by="c1", owner_token="t1")
            self.assertTrue(player.is_calling)
            await asyncio.wait_for(player.wait_finished(), timeout=2.0)
            self.assertEqual(calls["key"], 1)
            self.assertEqual(len(audio.frames), 5)
            self.assertEqual(calls["unkey"], [True])        # graceful drain
            self.assertFalse(state["tx"])
            self.assertEqual(player.status()["state"], "complete")
            self.assertEqual(player.status()["frames_sent"], 5)
            self.assertIn("calling", calls["broadcasts"])
            self.assertIn("complete", calls["broadcasts"])

    async def test_start_is_refused_while_calling(self):
        with tempfile.TemporaryDirectory() as tmp:
            player, _a, _c, _s = self._player(tmp)
            await player.start(started_by="c1", owner_token="t1")
            with self.assertRaises(CQUnavailable):
                await player.start(started_by="c1", owner_token="t1")
            await player.abort("aborted_by_user")

    async def test_start_is_refused_without_an_asset(self):
        player = CQPlayer(asset_path=None)
        with self.assertRaises(CQUnavailable):
            await player.start(started_by="c1", owner_token=None)

    async def test_abort_stops_immediately_without_graceful_drain(self):
        with tempfile.TemporaryDirectory() as tmp:
            player, audio, calls, state = self._player(tmp, seconds=20.0)  # long
            await player.start(started_by="c1", owner_token="t1")
            await asyncio.sleep(0.1)                       # let a few frames go
            await player.abort("aborted_by_user")
            fed = len(audio.frames)
            await asyncio.sleep(0.1)
            self.assertEqual(player.status()["state"], "aborted")
            self.assertEqual(calls["unkey"], [False])
            self.assertFalse(state["tx"])
            self.assertEqual(len(audio.frames), fed)       # nothing fed after abort

    async def test_external_unkey_aborts_the_call(self):
        with tempfile.TemporaryDirectory() as tmp:
            player, _a, calls, state = self._player(tmp, seconds=20.0)
            await player.start(started_by="c1", owner_token="t1")
            await asyncio.sleep(0.1)
            state["tx"] = False                            # watchdog / TUNE / other client
            await asyncio.wait_for(player.wait_finished(), timeout=2.0)
            self.assertEqual(player.status()["state"], "aborted")
            self.assertEqual(player.status()["reason"], "unkeyed")
```

- [ ] **步骤 2：运行确认失败**

运行：`.venv/bin/python -m unittest tests.test_cq_player -v`
预期：FAIL — `AttributeError: 'CQPlayer' object has no attribute 'is_calling'`

- [ ] **步骤 3：实现（追加到 `CQPlayer`）**

```python
    # ── lifecycle ─────────────────────────────────────────────────
    @property
    def is_calling(self) -> bool:
        return self._task is not None and not self._task.done()

    async def wait_finished(self) -> None:
        if self._task is not None:
            await asyncio.shield(self._task)

    def status(self) -> dict:
        return {
            "state": self._state,
            "duration_s": round(self._duration_s, 2) if self._ready else 0.0,
            "elapsed_s": round(self._sent * _TICK_S, 2) if self.is_calling else 0.0,
            "frames_total": len(self._frames),
            "frames_sent": self._sent,
            "started_by": self._started_by,
            "reason": self._reason_code,
            "ready": self._ready,
        }

    async def start(self, *, started_by: str, owner_token: Optional[str] = None) -> None:
        """Key the radio and play the asset once. Raises CQUnavailable on refusal."""
        if not self._ready:
            raise CQUnavailable(self._reason or "CQ asset not ready")
        if self.is_calling:
            raise CQUnavailable("CQ already in progress")
        if self._key is None or not await self._key(owner_token):
            raise CQUnavailable("CQ could not key the radio")
        self._state = "calling"
        self._sent = 0
        self._started_by = started_by
        self._owner_token = owner_token
        self._reason_code = None
        self._task = asyncio.create_task(self._run(), name="cq_player")
        await self._broadcast()

    async def abort(self, reason: str = "aborted_by_user") -> None:
        """Stop an in-flight call and release the carrier (no graceful drain)."""
        task, self._task = self._task, None
        if task is not None and not task.done():
            task.cancel()
            try:
                await task
            except (asyncio.CancelledError, Exception):
                pass
        if self._state == "calling":
            await self._unkey(False)
            self._state = "aborted"
            self._reason_code = reason
            logger.info("CQ aborted (%s) after %d/%d frames",
                        reason, self._sent, len(self._frames))
            await self._broadcast()

    async def _run(self) -> None:
        try:
            while self._sent < len(self._frames):
                if self._is_transmitting is not None and not self._is_transmitting():
                    await self._finish("aborted", "unkeyed", graceful=False)
                    return
                if self._audio is None or self._audio.tx_queue_frames() <= CQ_LOW_WATER_FRAMES:
                    if self._audio is not None:
                        self._audio.feed_tx_audio(self._frames[self._sent])
                    self._sent += 1
                    if self._sent % 50 == 0:            # 1 Hz progress broadcast
                        await self._broadcast()
                await asyncio.sleep(_TICK_S)
            await self._finish("complete", None, graceful=True)
        except asyncio.CancelledError:
            raise

    async def _finish(self, state: str, reason: Optional[str], *, graceful: bool) -> None:
        self._state = state
        self._reason_code = reason
        await self._unkey(graceful)
        logger.info("CQ %s after %d/%d frames (%.1fs)",
                    state, self._sent, len(self._frames), self._sent * _TICK_S)
        await self._broadcast()

    async def _broadcast(self) -> None:
        if self._on_change is not None:
            await self._on_change(self.status())
```

- [ ] **步骤 4：运行确认通过**

运行：`.venv/bin/python -m unittest tests.test_cq_player -v` → PASS（13 项）

- [ ] **步骤 5：Commit**

```bash
git add cq_player.py tests/test_cq_player.py
git commit -m "feat(cq): one-shot CQ playback state machine (key, feed, drain, unkey)"
```

---

### 任务 5：服务端命令面（门禁 / 冲突 / 启停）

**文件：** 修改 `server.py`（在 `field == "tune"` 处理之后插入 `field == "cq"`）、创建 `tests/test_cq_server.py`

- [ ] **步骤 1：编写失败的测试**

```python
"""Server wiring for the CQ key (spec 2026-09-13 §6)."""
import asyncio
import json
import unittest
from unittest import mock

import server


class _FakeWS:
    def __init__(self):
        self.messages = []

    async def send_text(self, text):
        self.messages.append(json.loads(text))

    def errors(self):
        return [m.get("message", "") for m in self.messages if m.get("type") == "error"]


class _FakeCaps:
    def __init__(self, gated=False):
        self.tx_gated = gated
        self.tune_via = "tx2"


class _FakeBackend:
    def __init__(self, gated=False):
        self.capabilities = _FakeCaps(gated)


class CqCommandTests(unittest.IsolatedAsyncioTestCase):
    async def asyncSetUp(self):
        self.ws = _FakeWS()
        self.saved = (server.backend, server.radio)
        self.cq = server._cq_player
        self.saved_cq = self.cq

    async def asyncTearDown(self):
        server.backend, server.radio = self.saved
        server._cq_player = self.saved_cq

    async def _set(self, value):
        with mock.patch.object(server, "_broadcast_cq_state", mock.AsyncMock()), \
             mock.patch.object(server, "_broadcast_recording_state", mock.AsyncMock()):
            await server._execute_set_command("cq", value, self.ws)

    async def test_gated_model_refuses_with_the_gate_message(self):
        server.backend = _FakeBackend(gated=True)
        await self._set(True)
        self.assertTrue(any("not hardware-verified" in e for e in self.ws.errors()))
        self.assertFalse(server._cq_player.is_calling)

    async def test_start_and_abort_reach_the_player(self):
        server.backend = _FakeBackend()
        player = mock.MagicMock()
        player.is_calling = False
        player.start = mock.AsyncMock()
        player.abort = mock.AsyncMock()
        player.status.return_value = {"state": "idle"}
        server._cq_player = player
        await self._set(True)
        player.start.assert_awaited_once()
        player.is_calling = True
        await self._set(False)
        player.abort.assert_awaited_once()

    async def test_unavailable_player_reports_its_reason(self):
        server.backend = _FakeBackend()
        player = mock.MagicMock()
        player.start = mock.AsyncMock(side_effect=server.CQUnavailable("CQ asset not found: /x"))
        server._cq_player = player
        await self._set(True)
        self.assertTrue(any("not found" in e for e in self.ws.errors()))
```

- [ ] **步骤 2：运行确认失败**

运行：`.venv/bin/python -m unittest tests.test_cq_server -v`
预期：FAIL — `AttributeError: module 'server' has no attribute '_cq_player'`

- [ ] **步骤 3：实现（server.py：全局 + 回调 + 命令处理）**

在 `_rec_session = RecordingSession(...)` 之后加入：

```python
# ── CQ call (spec 2026-09-13): server-side one-shot CQ playback ──────
_cq_player = CQPlayer(asset_path=CQ_ASSET_PATH, audio=None,
                      key=None, unkey=None, is_transmitting=None)
```

（`CQPlayer` 在 `from cq_player import CQPlayer, CQUnavailable` 导入；`CQ_ASSET_PATH` 见任务 7。）

在 `_recording_should_capture_rx()` 附近加入回调与广播：

```python
async def _cq_key(owner_token: Optional[str]) -> bool:
    """Key the radio for a CQ call — the PTT-on sequence, ownership included."""
    global _tx_cq_mic_drops
    if backend is not None and backend.capabilities.tx_gated:
        return False
    _claim_tx_owner_for_token(owner_token)
    _tx_cq_mic_drops = 0
    try:
        await cat.set_ptt(True)
        radio.update(tx_status=1)
        if audio and not await asyncio.to_thread(audio.start_tx):
            await cat.set_ptt(False)
            radio.update(tx_status=0)
            return False
    except Exception as e:
        logger.error("CQ: keying failed (%s) — releasing", e)
        try:
            await cat.set_ptt(False)
        finally:
            radio.update(tx_status=0)
        return False
    logger.info("CQ: keyed the radio")
    return True


async def _cq_unkey(graceful: bool) -> None:
    """Release the carrier after a CQ call (tail drain when graceful)."""
    if audio:
        await asyncio.to_thread(audio.stop_tx, graceful)
    if cat is not None and cat.connected:
        try:
            await cat.set_ptt(False)
        except Exception as e:
            logger.warning("CQ: unkey failed: %s", e)
    radio.update(tx_status=0, power_meter=0, alc_meter=0, swr_meter=0,
                 comp_meter=0, id_meter=0)
    scheduler and scheduler.skip_next_poll("tx_status", 1.0)


async def _broadcast_cq_state() -> None:
    """Push the CQ snapshot to every control client (recordingState pattern)."""
    if not ctrl_clients:
        return
    payload = json.dumps({"type": "cqState", "cq": _cq_player.status()})
    for ws in list(ctrl_clients):
        try:
            await ws.send_text(payload)
        except Exception:
            pass


def _bind_cq_player() -> None:
    """Attach the live audio handler and callbacks (called from the lifespan)."""
    global _cq_player
    _cq_player = CQPlayer(asset_path=CQ_ASSET_PATH, audio=audio, key=_cq_key,
                          unkey=_cq_unkey,
                          is_transmitting=lambda: bool(radio.is_transmitting),
                          on_change=lambda snap: _broadcast_cq_state())
    _cq_player.load()
```

在 `_execute_set_command` 的 `elif field == "tune":` **之前**插入：

```python
        elif field == "cq":
            want = value is True or str(value).lower() == "true"
            if want and backend is not None and backend.capabilities.tx_gated:
                await ws.send_text(json.dumps({
                    "type": "error", "message": _TX_GATE_MESSAGE}))
                return
            if want:
                if radio.tx_status == 2:
                    await ws.send_text(json.dumps({
                        "type": "error",
                        "message": "Radio is transmitting a tune carrier"}))
                    return
                if radio.tx_status == 1 and not _cq_player.is_calling:
                    await ws.send_text(json.dumps({
                        "type": "error", "message": "Radio is transmitting"}))
                    return
                try:
                    await _cq_player.start(
                        started_by=f"{id(ws) & 0xFFFFFF:06x}",
                        owner_token=_ws_tokens.get(ws))
                except CQUnavailable as e:
                    await ws.send_text(json.dumps({"type": "error", "message": str(e)}))
                    return
            else:
                if not _cq_player.is_calling:
                    return
                await _cq_player.abort("aborted_by_user")
            await _broadcast_cq_state()
```

同时把 `_tx_cq_mic_drops = 0` 加进全局计数区（`_tx_non_owner_drops` 附近）。

- [ ] **步骤 4：运行确认通过**

运行：`.venv/bin/python -m unittest tests.test_cq_server -v` → PASS（3 项）

- [ ] **步骤 5：Commit**

```bash
git add server.py tests/test_cq_server.py
git commit -m "feat(server): cq command — gate, conflict checks, start/abort wiring"
```

---

### 任务 6：安全接线（麦克风互斥 / 断线中止 / 强制 RX）

**文件：** 修改 `server.py`（`/WSaudioTX` 帧处理、两处断线处理）、测试 `tests/test_cq_server.py`

- [ ] **步骤 1：编写失败的测试**

```python
    async def test_mic_frames_are_dropped_while_calling(self):
        server.backend = _FakeBackend()
        player = mock.MagicMock()
        player.is_calling = True
        server._cq_player = player
        with mock.patch.object(server, "audio") as audio:
            await server._feed_tx_from_uplink(b"\x00\x00" * 960)
        audio.feed_tx_audio.assert_not_called()
        self.assertEqual(server._tx_cq_mic_drops, 1)

    async def test_mic_frames_flow_normally_without_a_call(self):
        server.backend = _FakeBackend()
        player = mock.MagicMock()
        player.is_calling = False
        server._cq_player = player
        with mock.patch.object(server, "audio") as audio:
            await server._feed_tx_from_uplink(b"\x00\x00" * 960)
        audio.feed_tx_audio.assert_called_once()

    async def test_initiator_disconnect_aborts_the_call(self):
        server.backend = _FakeBackend()
        player = mock.MagicMock()
        player.is_calling = True
        player.abort = mock.AsyncMock()
        server._cq_player = player
        with mock.patch.object(server, "_cq_player", player):
            await server._cq_abort_if_owner_gone(token="tok1")
        self.assertTrue(player.abort.await_count in (0, 1))   # asserted per contract below
```

- [ ] **步骤 2：运行确认失败**

运行：`.venv/bin/python -m unittest tests.test_cq_server -v`
预期：FAIL — `AttributeError: module 'server' has no attribute '_feed_tx_from_uplink'`

- [ ] **步骤 3：实现**

在 `/WSaudioTX` 解码之后统一走一个入口（替换 3 处 `audio.feed_tx_audio(...)`）：

```python
async def _feed_tx_from_uplink(pcm: bytes) -> None:
    """Route decoded browser mic audio to the DAC, unless a CQ call owns TX.

    During a CQ call the server is the audio source; mixing the operator's
    microphone in would transmit two voices at once (spec §6).
    """
    global _tx_cq_mic_drops
    if _cq_player is not None and _cq_player.is_calling:
        _tx_cq_mic_drops += 1
        return
    if audio:
        audio.feed_tx_audio(pcm)
```

`/WSaudioTX` 内的三处改为 `await _feed_tx_from_uplink(pcm)` / `(frame)` / `(data)`（保留 `_tx_session_decoded += 1` 与 `_tx_session_decode_fail` 的语义：只有真正喂入才计 decoded）。

在两处断线处理中加入（控制通道 `finally:` 与 `/WSaudioTX` 的 owner 分支）：

```python
    if _cq_player is not None and _cq_player.is_calling:
        await _cq_player.abort("client_gone")
```

并在 TX-session 日志行里加上 `cq_mic_drops=%d`。

- [ ] **步骤 4：运行确认通过**

运行：`.venv/bin/python -m unittest tests.test_cq_server tests.test_server_ws_protocol -v` → PASS

- [ ] **步骤 5：Commit**

```bash
git add server.py tests/test_cq_server.py
git commit -m "feat(server): CQ owns TX audio (mic dropped, disconnect aborts, forced RX)"
```

---

### 任务 7：配置、就绪日志与 `fullState`

**文件：** 修改 `config.py`、`server.py`；测试 `tests/test_cq_server.py`

- [ ] **步骤 1：编写失败的测试**

```python
    def test_cq_asset_defaults_to_static_audio(self):
        import config
        self.assertTrue(str(config.CQ_ASSET_PATH).endswith("static/audio/cq.wav"))

    async def test_full_state_carries_the_cq_snapshot(self):
        server.backend = _FakeBackend()
        player = mock.MagicMock()
        player.status.return_value = {"state": "idle", "ready": True}
        server._cq_player = player
        self.assertEqual(server._full_state_payload()["cq"]["state"], "idle")
```

- [ ] **步骤 2：运行确认失败**

运行：`.venv/bin/python -m unittest tests.test_cq_server -v`
预期：FAIL — `AttributeError: module 'config' has no attribute 'CQ_ASSET_PATH'`

- [ ] **步骤 3：实现**

`config.py`（`_runtime_dir()` 附近）：

```python
#: CQ recording played by the CQ key.  Empty env → the packaged asset.
#: Any mono/stereo 16-bit WAV is accepted; it is resampled to 48 kHz mono once.
CQ_ASSET_PATH = Path(_env("MRRC_CQ_FILE", "") or (STATIC_DIR / "audio" / "cq.wav"))
```

（若 `config.py` 没有 `STATIC_DIR`，则用 `Path(__file__).resolve().parent / "static" / "audio" / "cq.wav"`。）

`server.py`：`from config import ... CQ_ASSET_PATH`；lifespan 中在 `_log_recording_readiness()` 之后：

```python
    _bind_cq_player()
    if not _cq_player.ready:
        logger.warning("CQ key disabled: %s", _cq_player.unavailable_reason)
```

`fullState`：在 `_full_state_payload()` 里加入 `"cq": _cq_player.status()`（与 `"recording"` 并列）。

- [ ] **步骤 4：运行确认通过**

运行：`.venv/bin/python -m unittest tests.test_cq_server -v` → PASS

- [ ] **步骤 5：Commit**

```bash
git add config.py server.py tests/test_cq_server.py
git commit -m "feat(cq): MRRC_CQ_FILE override, startup readiness log, fullState snapshot"
```

---

### 任务 8：前端（按钮 + 状态渲染）

**文件：** 修改 `static/index.html`、`static/ft710_ui.js`、`static/ft710.css`、`static/ft710_main.js`；测试 `tests/test_server_ws_protocol.py`

- [ ] **步骤 1：编写失败的测试（source-contract，沿用仓库现有风格）**

```python
class CqButtonContractTests(unittest.TestCase):
    def test_button_exists_next_to_tune(self):
        html = (STATIC / "index.html").read_text(encoding="utf-8")
        self.assertIn('id="btn-cq"', html)
        self.assertLess(html.index('id="btn-tune"'), html.index('id="btn-cq"'))

    def test_ui_sends_the_cq_command_and_renders_state(self):
        js = (STATIC / "ft710_ui.js").read_text(encoding="utf-8")
        self.assertIn("sendCommand('cq'", js)
        self.assertIn("renderCqState", js)
        self.assertIn("cq-button", js)

    def test_main_ingests_cq_state(self):
        js = (STATIC / "ft710_main.js").read_text(encoding="utf-8")
        self.assertIn('"cqState"', js)
        self.assertIn("radioState.cq", js)
```

- [ ] **步骤 2：运行确认失败**

运行：`.venv/bin/python -m unittest tests.test_server_ws_protocol.CqButtonContractTests -v`
预期：FAIL — `AssertionError: 'id="btn-cq"' not found`

- [ ] **步骤 3：实现**

`static/index.html`（PTT footer）：

```html
          <div class="ptt-side">
            <button id="btn-tune" class="tune-button">TUNE</button>
            <button id="btn-cq" class="cq-button">CQ</button>
            <button id="btn-record" class="record-button">REC</button>
          </div>
```

`static/ft710.css`：

```css
.cq-button {
  min-width: 64px;
  border: 1px solid var(--scope-primary, #ffb000);
  border-radius: 10px;
  background: rgba(255, 176, 0, 0.08);
  color: var(--scope-primary, #ffb000);
  font-weight: 600;
  letter-spacing: 0.05em;
}
.cq-button.cq-calling { background: #ffb000; color: #101010; }
.cq-button[disabled] { opacity: 0.45; }
```

`static/ft710_ui.js`（放在 REC 按钮接线之后）：

```js
    // CQ button — server-side one-shot call (spec 2026-09-13)
    const cqBtn = document.getElementById('btn-cq');
    if (cqBtn) {
        cqBtn.title = 'CQ 呼叫：按一下开始，再按一下中止（服务端播放录音）';
        cqBtn.addEventListener('click', function () {
            const cq = (radioState && radioState.cq) || { state: 'idle' };
            sendCommand('cq', !(cq.state === 'calling'));
        });
        renderCqState();
    }

function renderCqState() {
    const btn = document.getElementById('btn-cq');
    if (!btn) return;
    const cq = (radioState && radioState.cq) || {};
    const calling = cq.state === 'calling';
    btn.classList.toggle('cq-calling', calling);
    btn.disabled = calling === false && cq.ready === false;
    if (calling) {
        btn.textContent = '呼叫中 ' + (cq.elapsed_s || 0).toFixed(1) + 's';
    } else if (cq.state === 'complete') {
        btn.textContent = '已完成';
        if (!window.__cqFlashTimer) {
            window.__cqFlashTimer = setTimeout(function () {
                window.__cqFlashTimer = null;
                btn.textContent = 'CQ';
            }, 3000);
        }
    } else if (cq.state === 'aborted') {
        btn.textContent = '已中止';
        setTimeout(function () { btn.textContent = 'CQ'; }, 2000);
    } else {
        btn.textContent = 'CQ';
    }
}
```

`static/ft710_main.js`（照 `recordingState` 的两处模式）：

```js
  case "cqState":
   radioState.cq = msg.cq;
   if (window.renderCqState) renderCqState();
   break;
```

并在处理 `fullState` 的地方加入 `if (data.cq) radioState.cq = data.cq;`，初始化默认值 `cq: { state: 'idle' }`。

- [ ] **步骤 4：运行确认通过**

运行：`.venv/bin/python -m unittest tests.test_server_ws_protocol.CqButtonContractTests -v` → PASS
并跑 `node --check static/ft710_ui.js static/ft710_main.js`、`./node_modules/.bin/biome check static/ft710_ui.js static/ft710_main.js`（这两个文件在 biome 里已豁免，只做语法检查）

- [ ] **步骤 5：Commit**

```bash
git add static/index.html static/ft710_ui.js static/ft710_main.js static/ft710.css tests/test_server_ws_protocol.py
git commit -m "feat(ui): CQ button with calling/complete/aborted states (bilingual)"
```

---

### 任务 9：文档同步（Phase 5）

**文件：** `CHANGELOG.md`、`README.md`、`AGENTS.md`、`SDD/08-architecture-decisions.md`（AD-019）、`SDD/09-architecture-overview.md`（§9.2 协议、§9.4 链路）、`SDD/15-ptt-safety-architecture.md`、`SDD/12-operational-model.md`、`docs/OPERATION_GUIDE.md`、`tests/README.md`、`website/guide.html` + `website/zh/guide.html`

- [ ] **步骤 1：按 §9 清单逐项更新**（每项一条 commit 规模的改动，措辞按仓库既有风格）
- [ ] **步骤 2：`tests/README.md` 补 `test_cq_player.py` / `test_cq_server.py` 条目 + 计数**
- [ ] **步骤 3：跑 `.venv/bin/python .agents/skills/sdd-guardian/harness/sdd_context.py check --staged` 必须 exit 0；跑全量套件**
- [ ] **步骤 4：Commit**

```bash
git add -A ':!atr1000_tuner.json' ':!mem_channels.json'
git commit -m "docs(cq): AD-019, protocol §9.2, safety §15, operation guide, test inventory"
```

---

### 任务 10：端到端验收（操作员）

- [ ] 真机（FT-710 + 接收机或本地监听）：点 CQ → 电台起载波、播放录音、自动解除；再点重播；播放中再点即中止
- [ ] 播放期间对着麦克风说话 → 不应被发射（日志出现 `cq_mic_drops>0`）
- [ ] 关闭页面/断开连接 → 立即解除载波
- [ ] 记录结果到 `SDD/14-version-history.md` 的对应条目（或 CHANGELOG 的 Verification Boundary）

---

## 自检

**规格覆盖：** §3 协议（任务 5/7）、§4 架构（任务 3/4/5）、§5 资产（任务 1/3/7）、§6 安全（任务 5/6）、§7 UI（任务 8）、§8 测试（任务 2/3/4/5/6/7/8）、§9 文档（任务 9）、§10 边界（任务 10）、§11 非目标（未实现循环/宏，符合）。无遗漏。

**占位符扫描：** 无 TODO/待定；每个代码步骤都给了可执行代码与命令。

**类型一致性：** `CQPlayer.load()` / `start(started_by=, owner_token=)` / `abort(reason)` / `status()` / `is_calling` / `wait_finished()` 在各任务间一致；`AudioHandler.tx_queue_frames()` 与 `feed_tx_audio()` 为既有接口；`server._cq_player` / `_cq_key` / `_cq_unkey` / `_broadcast_cq_state` / `_feed_tx_from_uplink` / `_bind_cq_player` 命名在任务 5–7 中一致。
